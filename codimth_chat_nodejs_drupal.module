<?php

/**
 * Implements hook_theme() to add the template definition.
 **/
function codimth_chat_nodejs_drupal_theme($existing, $type, $theme, $path) {
    return array(
        'chat' => array(
            'variables' => array(
                'users' => NULL
            ),
        ),
    );
}
